ckpt/
*.pt
*.npz
shake.txt
# the marine model is the deliverable of that run, so it ships
!language/models/*.pt
__pycache__/
*.pyc
.venv/

# per-directory run artefacts; canonical copies live in results/
mazes/*.json
mazes/*.log
language/*.json
language/*.log
precision/*.json
precision/*.log
